formatversion: 1.0.0

info:
  name: stock_test
  summary: One line summary
  description: |
    In depth description of stock_test
    With no details.
  authors: ['Martin Davis <martindavis@exosite.com>']
  version: 1.0.0

assets:
  location: assets
  include: '**/*'
  exclude: ['something_spooky.html']
  default_page: index.html

endpoints:
  location: endpoints
  include: '**/*.lua'
  exclude: ['*_test.lua', '*_spec.lua']
  cors: {'origin': ['http://localhost:*']}

modules:
  location: modules
  include: '**/*.lua'
  exclude: ['*_test.lua', '*_spec.lua']

services:
  location: services
  include: '**/*.lua'
  exclude: ['*_test.lua', '*_spec.lua']

init_script: init.lua
